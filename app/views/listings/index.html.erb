<% content_for :title, "All Listings" %>

<%= react_component "ListingsIndexPage",
  {
    listings: listing_card_payloads(@listings),
    meta: { total: @listings.count },
    actions: {
      can_create: user_signed_in?,
      new_listing_path: new_listing_path,
      sign_in_path: new_user_session_path
    },
    paths: {
      home: root_path
    }
  } %>
