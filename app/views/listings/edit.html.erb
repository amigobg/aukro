<% content_for :title, "Edit Listing" %>

<%= react_component "ListingFormPage",
  listing_form_props(
    listing: @listing,
    heading: "Update listing",
    subheading: "Refine details, adjust pricing, or refresh the description.",
    submit_label: "Save changes",
    action: listing_path(@listing),
    method: :patch
  )
%>
