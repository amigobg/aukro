<% content_for :title, @listing.title %>

<%= react_component "ListingShowPage",
  {
    listing: listing_detail_payload(@listing),
    authenticity_token: form_authenticity_token,
    paths: {
      back: listings_path
    }
  } %>
