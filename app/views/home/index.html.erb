<%= react_component "HomePage",
  {
    categories: category_payloads(@categories),
    featured_listings: listing_card_payloads(@featured_listings),
    paths: {
      all_categories: categories_path,
      listings: listings_path,
      new_listing: new_listing_path
    }
  } %>
